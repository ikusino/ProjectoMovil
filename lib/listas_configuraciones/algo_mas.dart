import 'package:flutter/material.dart';

class AlgoMas extends StatelessWidget {
  const AlgoMas({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('algo')),
      body: Center(
        child: Text(
          'nunca hace falta que quieran añadir algo xd',
        ),
      ),
    );
  }
}
